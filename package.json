{
  "name": "postcode-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.0.0"
  },
  "dependencies": {
    "ajv": "^8.20.0"
  }
}
